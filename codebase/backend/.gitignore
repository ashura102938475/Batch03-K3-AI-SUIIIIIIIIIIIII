.env
.venv/
__pycache__/
*.pyc

# Data pack safety — do not commit raw data pack files
corpus/*.pdf
corpus/transcript-*.md

# Runtime traces are local-only. Evaluation evidence belongs in the root eval/ folder.
runs/*.json
runs/*.jsonl
